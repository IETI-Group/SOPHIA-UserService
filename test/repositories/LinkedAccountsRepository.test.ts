import { beforeEach, describe, expect, it } from 'vitest';

describe('Linked Accounts Repository', () => {
  beforeEach(() => {});
  it('should create a new linked account', () => {
    expect(true).toBe(true);
  });
  it('should find a linked account by ID', () => {
    expect(true).toBe(true);
  });
});
